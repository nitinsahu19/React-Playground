const Wrapper = (props) => {
    return props.children;
}

export default Wrapper;




// NOTE => 
// Pass props as an argument in the component where you are sending props, and import modules in the component from which you are sending props.